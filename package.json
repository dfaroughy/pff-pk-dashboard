{
  "name": "pff-pk-dashboard",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev:empirical": "npm --prefix apps/empirical run dev:portable:full",
    "dev:synthetic": "npm --prefix apps/synthetic run dev:portable",
    "build": "npm run build:empirical && npm run build:synthetic",
    "build:empirical": "npm --prefix apps/empirical run build:portable",
    "build:synthetic": "npm --prefix apps/synthetic run build:portable",
    "build:pages": "npm run build && node scripts/assemble-pages.mjs",
    "test:pages": "node --test tests/pages-bundle.test.mjs",
    "check": "npm --prefix apps/empirical run lint && npm --prefix apps/empirical test && npm --prefix apps/synthetic run check && npm run build:pages && npm run test:pages"
  }
}
