# Shared visual system

Both dashboards use Space Grotesk for interface text and IBM Plex Mono for
scientific metadata. Their current styles remain application-local to avoid a
visual regression during repository consolidation. New shared primitives
should be introduced here only with visual regression coverage in both apps.
